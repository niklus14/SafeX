# Copy to .env and fill in. Never commit .env.
# Real Grok intake (leave OPENWAVE_MOCK unset when using this):
XAI_API_KEY=

# Set to 1 for deterministic fake intake with no API key (frontend dev):
OPENWAVE_MOCK=1
